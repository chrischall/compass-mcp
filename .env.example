# No credentials needed. Auth lives in your signed-in compass.com browser
# tab via the fetchproxy extension. This file is a marker only.
#
# Optional overrides:
# COMPASS_WS_PORT=37149              # fetchproxy WebSocket port
# COMPASS_COMMUNITIES_FILE=          # path to a JSON array of community names
#                                    # (overrides the built-in vocabulary)
